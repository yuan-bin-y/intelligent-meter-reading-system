<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.byy.meterreading.mapper.SysRoleMapper">

    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        SELECT role.role_code
        FROM sys_role AS role
        INNER JOIN sys_user_role AS user_role
                ON user_role.role_id = role.id
        WHERE user_role.user_id = #{userId}
          AND role.status = 1
        ORDER BY role.id
    </select>

    <!-- 批量查询当前页用户的有效角色，避免逐个用户查询产生 N+1 问题。 -->
    <select id="selectRoleCodesByUserIds"
            resultType="com.byy.meterreading.mapper.projection.UserRoleCodeRow">
        SELECT ur.user_id  AS userId,
               r.role_code AS roleCode
        FROM sys_user_role AS ur
        INNER JOIN sys_role AS r
                ON r.id = ur.role_id
        WHERE ur.user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
          AND r.status = 1
        ORDER BY ur.user_id, r.id
    </select>

</mapper>
